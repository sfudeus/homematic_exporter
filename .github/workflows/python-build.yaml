name: Python package

on:
  push:
    branches:
    - master
    - main
  pull_request:
    branches:
    - master
    - main

jobs:
  build:
    uses: sfudeus/workflows/.github/workflows/python-build.yaml@main
    secrets: inherit
